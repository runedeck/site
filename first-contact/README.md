# first contact

The guided first-run walkthrough of runedeck: install, orient, deploy, trust, author, adopt. It becomes a subsite here once a design is picked from `../designs/`, built in the winning design's language.

The content already exists as the presenter script for the seminar live build; this directory carries it to the web.
